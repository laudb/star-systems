mixin system-form
    div(class="modal" id="addModal")
        form(action="/system", method="post" class="modal-content")
            span(class="close")
                X
            input#system-name(type="name", name="system-name")
            if err
                p Error: #{err}
            button(type="submit") Submit