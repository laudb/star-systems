extend layout

block content
  section(class='hero is-medium')
    div(class='hero-body')
      div(class='container')
          h1(class='title') #{title}
          ul
            li= system
  section(class='hero is-small')
    div(class='hero-body')
      div(class='container')
      +system-form()(class='modal')


      